sudo: false
language: c++
addons:
  apt:
    packages:
    - scons
    - gcc-4.8
    - libeigen3-dev
    - libprotobuf-dev
    - protobuf-compiler
install:
  - uname -a
  - g++ -version
script:
  - scons
